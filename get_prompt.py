import json
import urllib.request
import urllib.parse
from pathlib import Path

# Actually, the user already provided the solution! 
# The UI JSON contains the exact nodes and properties. But wait, I can just use playwright.
